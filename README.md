# AI Plugins

Clean source snapshots from a local Codex `plugins` directory.

Backup files, runtime status, private local configuration, and generated cache artifacts are intentionally excluded from version control.
