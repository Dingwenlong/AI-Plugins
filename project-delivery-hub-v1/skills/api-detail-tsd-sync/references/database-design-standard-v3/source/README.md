# Database Design Standard v3 Source

This folder records conversion status for `數據庫設計規範 v3 20220908.docx`.

The current supplied file is not a runtime-readable reference source. It is an old Office OLE container with DRM/encrypted package streams, so the skill must not derive database design rules from it.

Runtime behavior is controlled by `../catalog.json`. Until readable source text is supplied, database-table-design tasks should load only the blocker rule and mark the v3 database-design standard as `source_unreadable`.
